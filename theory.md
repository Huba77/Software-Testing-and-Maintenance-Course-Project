# Donut 论文理论整理（成员B）

## 1. KDE 解释（Kernel Density Estimation）

### 核心思想

Donut 使用 VAE 学习正常数据的分布。在 latent space 中，正常数据点会聚集在某些区域（高密度区），而异常数据点会落在低密度区域。

### 数学表达

VAE 的 encoder 学习的是后验分布 $q(z|x)$，decoder 学习的是条件分布 $p(x|z)$。

对于正常数据，$q(z|x)$ 应该接近标准正态先验 $p(z) = \mathcal{N}(0, I)$。

异常检测的核心假设：
- **正常点**：$x$ 可以被 VAE 很好地重构，$p(x|z)$ 概率高
- **异常点**：$x$ 难以被重构，$p(x|z)$ 概率低

### KDE 在 Donut 中的应用

Donut 的 anomaly score 本质上是基于重构概率的：

$$
\text{score}(x) = -\mathbb{E}_{q(z|x)}[\log p(x|z)]
$$

这等价于在 latent space 中，用采样来估计 $x$ 的"正常程度"。

正常数据的 latent 表示 $z$ 会集中在先验分布的高密度区域，而异常数据的 $z$ 会落在尾部（低密度区），导致重构误差大。

### 为什么 KDE 有效

1. **非参数密度估计**：不需要假设数据分布的具体形式
2. **Latent space 平滑**：decoder 作为神经网络，在 latent space 中提供平滑的密度估计
3. **局部敏感性**：对局部异常模式敏感

---

## 2. Time Gradient 效应

### 现象描述

在 KPI 异常检测中，存在一个关键现象：**异常分数在异常开始时会逐渐增大，而不是突变**。

这被称为 **Time Gradient Effect（时间梯度效应）**。

### 产生原因

1. **滑动窗口机制**：Donut 使用滑动窗口，异常开始只影响窗口的一部分
2. **VAE 的平滑重构**：VAE 倾向于输出"平均化"的结果，对突变不敏感
3. **上下文依赖**：窗口中的正常点会"拉低"整体异常分数

### 影响

- **Alert Delay**：检测到异常需要一定延迟
- **分数曲线平滑**：异常分数呈渐变而非阶跃
- **阈值选择困难**：需要权衡灵敏度和误报率

### 应对策略

1. **Segment-based 评估**：允许一定时间偏移，不严格要求逐点对齐
2. **动态阈值**：使用百分位数阈值（如 95th percentile）
3. **多步预测**：考虑时间序列的未来信息

---

## 3. Segment-based 调整策略详解

### 动机

KPI 异常检测不同于传统分类：
- 异常通常是**连续区间**而非孤立点
- 只要检测到异常区间的**任何一部分**，就算成功检测
- 逐点评估过于严格，不符合实际运维需求

### 策略规则

**预测调整（Prediction Adjustment）**：
- 如果一个预测异常段中命中了任何真实异常 → 整个段视为正确
- 如果一个预测异常段完全未命中 → 整个段视为误报

**真实标签调整（Ground Truth Adjustment）**：
- 如果一个真实异常段被任何预测异常段覆盖 → 整个段视为被检测到

### 效果

- **Precision**：降低（更多预测被视为正确）
- **Recall**：提高（更多异常被成功检测）
- **F1**：通常提高，更符合实际评估需求

---

## 4. M-ELBO 的数学原理

### 标准 ELBO

$$
\mathcal{L}_{\text{ELBO}} = \mathbb{E}_{q(z|x)}[\log p(x|z)] - \beta \cdot D_{KL}(q(z|x) \| p(z))
$$

### Modified ELBO (M-ELBO)

Donut 引入 mask $\alpha_w$ 来屏蔽异常/缺失点：

$$
\mathcal{L}_{\text{M-ELBO}} = \mathbb{E}_{q(z|x)}\left[\sum_{t \in W} \alpha_w^{(t)} \log p(x^{(t)}|z)\right] - \beta \cdot D_{KL}(q(z|x) \| p(z))
$$

其中：
- $\alpha_w^{(t)} = 1$：时间点 $t$ 是正常点
- $\alpha_w^{(t)} = 0$：时间点 $t$ 是异常/缺失点

### 为什么有效

1. **防止异常点污染训练**：异常点不参与重构损失，模型只学习正常模式
2. **缺失值鲁棒性**：随机置零模拟缺失，模型学会从不完整数据推断
3. **β 控制正则化**：平衡重构质量和 latent space 约束

---

## 5. MCMC Imputation 原理

### 核心思想

检测阶段，对于可能存在异常的窗口：
1. **固定观测点**：确信为正常的点保持不变
2. **迭代填补**：对可疑位置用模型预测进行多次采样
3. **平均结果**：降低随机性，获得更稳定的重构

### 简化实现

本项目的 MCMC 是简化版：
- 用重构误差识别"可疑位置"
- 对这些位置进行多次采样和迭代 refine
- 返回平均重构结果

### 完整版（论文）

论文中的 MCMC 更严格：
- 使用真实的 missing mask（已知缺失位置）
- 固定观测点，对缺失点进行条件采样
- 迭代 $M$ 步使采样收敛

---

## 6. PPT 技术原理要点

### 给听众的关键信息

1. **问题**：KPI 异常检测的挑战
   - 数据高维、高噪声
   - 异常模式多样
   - 标注困难/昂贵

2. **方法**：Donut 的核心创新
   - VAE + 滑动窗口 → 学习正常模式
   - M-ELBO → 屏蔽异常，只学正常
   - Missing Injection → 增强对缺失的鲁棒性
   - MCMC Imputation → 检测时更稳定的重构

3. **评估**：为什么用这些指标
   - AUC：整体排序能力
   - Best F1：最优阈值下的性能
   - Alert Delay：实际运维最关心的延迟
   - Segment-based：符合异常区间的实际形态

4. **消融实验**：证明每个组件的价值
   - VAE Baseline → 基础能力
   - +M-ELBO → mask 的提升
   - +Injection → 缺失鲁棒性
   - +MCMC → 检测稳定性
