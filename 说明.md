# Donut 论文复现项目交接说明（给成员B）

## 1. 当前项目状态

目前已经完成：

* Donut 基础框架
* 数据读取
* 标准化
* 滑动窗口
* VAE Encoder/Decoder
* Reparameterization
* ELBO Loss
* Missing Injection
* 模型训练
* anomaly score 输出

项目已经成功跑通完整 pipeline：

```text
train.csv
→ train.py
→ donut_model.pth
→ detect.py
→ anomaly_scores.csv
```

目前可以正常输出 anomaly score。

---

# 2. 项目目录结构

```text
donut_project/
│
├── train.py
├── detect.py
├── model.py
├── loss.py
├── data.py
├── config.py
├── requirements.txt
└── README.md
```

---

# 3. 运行方式

## 安装依赖

```bash
pip install -r requirements.txt
```

---

## 训练

```bash
python train.py --train_csv train.csv
```

输出：

```text
donut_model.pth
```

---

## 检测

```bash
python detect.py --test_csv train.csv
```

输出：

```text
anomaly_scores.csv
```

---

# 4. 当前数据格式

CSV格式：

```csv
timestamp,value
2024-01-01 00:00:00,100
```

如果没有 timestamp 列：

data.py 已经兼容。

---

# 5. 当前核心实现

## Donut结构

当前实现是：

```text
滑动窗口 + VAE
```

不是 RNN/LSTM。

---

## 当前参数

config.py：

```python
WINDOW_SIZE = 120
LATENT_DIM = 3
HIDDEN_DIM = 100
```

测试时我改过：

```python
WINDOW_SIZE = 5
```

正式实验建议改回：

```python
WINDOW_SIZE = 120
```

---

# 6. 已完成的 Donut 特性

## 已完成

### Missing Injection

训练时随机置零：

```python
mask = np.random.rand(*x.shape) < rate
x[mask] = 0
```

---

### ELBO Loss

当前：

```python
loss = recon_loss + 0.01 * kl_loss
```

---

### anomaly score

当前：

```python
MSE reconstruction error
```

即：

```python
((x - recon_x)**2).mean()
```

---

# 7. 当前结果

模型已经能正常输出 anomaly score。

异常区域 score 明显增大。

训练 loss 能正常下降。

---

# 8. 你接下来需要完成的部分（重点）

## 第一优先级（必须）

### 1. 保存 loss 曲线

train.py：

```python
loss_history.append(avg_loss)
```

然后：

```python
plt.plot(loss_history)
```

保存：

```text
loss_curve.png
```

---

### 2. anomaly score 曲线

detect.py：

```python
plt.plot(scores)
```

保存：

```text
anomaly_scores.png
```

这是 PPT 核心实验图。

---

## 第二优先级（实验指标）

### 3. ROC / AUC / F1

需要：

* ground truth label
* sklearn.metrics

主要：

```python
roc_auc_score
f1_score
precision_score
recall_score
```

---

### 4. 阈值选择

例如：

```python
threshold = np.percentile(scores, 95)
```

超过 threshold 判异常。

---

## 第三优先级（论文增强，可选）

### 5. M-ELBO mask

目前是简化版。

可以进一步实现：

* abnormal point masking
* missing point masking

---

### 6. MCMC Imputation

目前未实现。

可选。

---

### 7. latent visualization

论文 Figure 10/12。

可以：

* latent_dim 改成 2
* TSNE/PCA 可视化 z

---

# 9. 当前项目实际完成度

基础复现：

```text
已经完成
```

剩余主要是：

* 实验结果
* 可视化
* 指标评估
* PPT包装
